# generated by install.sh (contains an absolute, machine-specific path)
examples/settings.generated.json

# local state / logs (default CTC_HOME lives outside the repo, but ignore here too)
*.jsonl
flags/
.cache/

# editor / OS noise
.DS_Store
*.swp
*~

# editor workspace settings
.vscode/
